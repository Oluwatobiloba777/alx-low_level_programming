#include <stdio.h>
/**
 *main- it runs the printf functions which prints it code block
 *
 *Return: 0 if successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
